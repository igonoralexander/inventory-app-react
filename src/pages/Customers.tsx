import React from 'react';
import { Typography, Box } from '@mui/material';

const Customers = () => {
  return (
    <Box>
      <Typography variant="h4">Customers</Typography>
      {/* Content for customers will go here */}
    </Box>
  );
};

export default Customers;